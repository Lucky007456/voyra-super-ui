/**
 * Server-side Supabase client — re-exports from the canonical utils location.
 * Kept for backward compatibility with existing API routes.
 */
export { createClient as createServerSupabaseClient } from '@/utils/supabase/server'
